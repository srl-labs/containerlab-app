{
  "name": "containerlab-app",
  "version": "0.3.0",
  "private": true,
  "description": "Monorepo for Containerlab web, desktop, VS Code extension, and shared UI.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "ui": "pnpm --filter @containerlab/clab-ui run build",
    "ui:local": "pnpm --filter @containerlab/clab-ui run dev:ui-harness",
    "ui:pack": "pnpm --filter @containerlab/clab-ui pack --out artifacts/clab-ui.tgz",
    "vsix": "pnpm ui && pnpm --filter vscode-containerlab run package",
    "vsix:local": "pnpm vsix",
    "web": "pnpm ui && pnpm --filter containerlab-app-web run build",
    "web:local": "pnpm ui && pnpm --filter containerlab-app-web run dev",
    "desktop": "pnpm ui && pnpm --filter containerlab-desktop run package",
    "desktop:local": "pnpm ui && pnpm --filter containerlab-desktop run dev",
    "desktop:windows": "node apps/desktop/scripts/dev-windows.mjs",
    "pages": "pnpm ui && pnpm --filter containerlab-app-web-public run build",
    "pages:local": "pnpm --filter containerlab-app-web-public run dev",
    "check": "node scripts/check-release-version.mjs && node scripts/check-deps.mjs",
    "typecheck": "pnpm ui && pnpm check && pnpm -r --if-present run typecheck",
    "lint": "pnpm ui && pnpm knip && pnpm -r --if-present run lint",
    "knip": "knip --treat-config-hints-as-errors",
    "test": "pnpm ui && node --test scripts/release-targets.test.mjs && pnpm -r --if-present run test:unit",
    "test:ui": "pnpm --filter @containerlab/clab-ui run test:browser",
    "test:web": "pnpm ui && pnpm --filter containerlab-app-web run test:e2e",
    "test:vscode": "pnpm ui && pnpm --filter vscode-containerlab run test:e2e",
    "test:package": "pnpm ui:pack && node scripts/check-packed-clab-ui.mjs artifacts/clab-ui.tgz",
    "sync:schema": "node scripts/sync-clab-schema.mjs",
    "preinstall": "node scripts/check-package-manager.mjs",
    "test:pages": "pnpm ui && pnpm --filter containerlab-app-web-public run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "knip": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "yaml": "catalog:"
  },
  "packageManager": "pnpm@12.4.2"
}
